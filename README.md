# Nanopore pipeline

This repo tracks:
- `analysis_setup/` — Snakemake pipeline & scripts
- `annotations/` — small helper annotation files (no big genomes)
- `data/example_pod5/500_example.pod5` — tiny example only

Large data/models/outputs live outside the repo and are ignored by design.
